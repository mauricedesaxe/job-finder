# Build config applies to every service deploying this file. The start command
# and health check are per-service settings in Railway, because one image hosts
# three roles: review app, Dagster webserver, and Dagster daemon.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
