[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'uv run --no-sync uvicorn scripts.serve_review:create_app --factory --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
